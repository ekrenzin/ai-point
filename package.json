{
  "name": "open-site",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "cross-env NODE_ENV=test mocha --recursive --exit",
    "ssh": "ssh -i opensite.pem ubuntu@ec2-35-86-117-103.us-west-2.compute.amazonaws.com",
    "clear-docker": "docker stop eankrenzin/open-site && docker rm open-site",
    "build-docker": "docker build -t open-site .",
    "run-docker": "docker run -p 8080:8080 -d open-site",
    "restart-docker": "npm run clear-docker && npm run build-docker && npm run run-docker",
    "start-docker": "npm run build-docker && npm run run-docker"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ekrenzin/open-site.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ekrenzin/open-site/issues"
  },
  "homepage": "https://github.com/ekrenzin/open-site#readme",
  "dependencies": {
    "assert": "^2.0.0",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "node-fetch": "^3.3.1",
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "cross-env": "^7.0.3",
    "mocha": "^10.2.0",
    "supertest": "^6.3.3",
    "typescript": "^5.0.4"
  }
}
